# Mandelbrot & Julia Sets

The Mandelbrot set is defined by iteration:

\[
z_{n+1} = z_n^2 + c, \quad z_0 = 0
\]

Each value of \( c \) defines a corresponding Julia set \( J(c) \).  
The Mandelbrot set maps the *stability* of those Julia sets.
